# We Rise DFW —— 用户指南

> **翻译版本：** [English](USER_GUIDE.md) · [Español](USER_GUIDE.es.md) · [العربية](USER_GUIDE.ar.md) · [中文](USER_GUIDE.zh.md)

一份用通俗语言写成的应用使用说明，无需任何技术背景。

---

## 这款应用的用途

We Rise DFW 帮助你在达拉斯 / 沃思堡（DFW）地区找到附近提供 **食物**、**衣物** 或 **临时住处** 的地方。点一下按钮，它就会显示你身边目前可用的资源——并附上地址、电话、营业时间和网站。

无论你的处境是短暂的、长期的，还是仅仅是难熬的一天，本应用都能用得上。无需登录。无需账号。它不会追踪你。

---

## 主屏幕上有什么

打开 We Rise DFW 时，屏幕从上到下分为以下几个区域：

**顶栏** —— 应用名称，右侧是设置图标（齿轮 ⚙）。

**地图**（屏幕上半部）—— DFW 地区的地图。完成一次搜索后，地图会显示彩色圆点表示附近的地点：
- 🟢 **绿色圆点** = 当前营业中
- 🔴 **红色圆点** = 当前已关闭

**标签栏** —— 中部三个标签：**食物**、**衣物**、**庇护所**。点选其一即可只看该类型的提供机构，地图也会随之只显示该类别的大头针。

**列表** —— 标签下方是当前类别中提供机构的可滚动列表。每张卡片显示名称、地址、距离、当前营业状态以及简短的描述。

**操作栏**（位于页脚正上方）—— 并排的两个按钮：
- **在线查找更多** —— 在互联网上查找额外的服务提供机构（详见下文）。
- **搜索** —— 根据当前位置进行一次新的搜索。

**页脚** —— 屏幕最下方一行小字"© We Rise DFW"。

---

## 如何执行搜索

1. 点击操作栏上的 **搜索**。
2. 第一次点击时，Android 会询问是否允许定位访问。请点击 **允许**（或 **使用应用时允许**）。如果拒绝，应用将无法搜索；之后可以在手机的应用设置中重新开启。
3. 应用会读取一次你的位置，找到所选半径范围内的提供机构，并填充列表与地图。

就是这样。任何时候想刷新结果，再次点击 **搜索** 即可。

> **重要提示：** 你的位置 **仅在你点击"搜索"的瞬间被读取**。它不会保存到手机里、不会发送到服务器、也不会被记录。下次再点击搜索时，应用会重新读取你的位置。

如果你不在 DFW 都会区内，地图上会出现 **超出范围** 的提示。本应用仅覆盖 DFW 地区。

---

## 如何使用提供机构卡片

点击列表中任意卡片即可进入 **详情页**。在详情页可以：

- **点击地址** —— 在你常用的地图应用中打开导航。
- **点击电话号码** —— 在手机的拨号应用中打开，号码已填好等待呼叫。
- **点击网站** —— 在浏览器中打开该机构的网站。

你还可以看到：
- 提供机构的全名以及当前是否营业。
- 如果是 **信仰背景** 机构，会显示一个十字图标 "✝"。（你可以在设置里选择是否隐藏。）
- 关于该机构所提供服务的简短介绍。
- 完整的每周营业时间。

点击左上角的返回箭头即可回到主列表。

---

## "在线查找更多" —— 在互联网上搜索

应用内置的列表是手工挑选的，但并不包含所有信息。**在线查找更多** 按钮会向 **OpenStreetMap**（一个由社区维护的免费地图数据库）询问你附近的更多服务提供机构。

**何时使用：**
- 内置列表在你所在的具体区域内信息较少。
- 你想看到精选集合之外的更多选择。
- 你身处 DFW 边缘地带，内置列表覆盖不全。

**点击后会发生什么：**
1. 按钮会显示"正在搜索 OpenStreetMap…"。
2. 网络上找到的新机构会与内置列表合并，一同显示。
3. 出现简短提示，告诉你新增了多少条（例如"已从 OpenStreetMap 添加 7 个新机构"）。

**关于在线结果需注意：**
- 部分细节可能缺失。OpenStreetMap 由志愿者维护，因此电话、营业时间和地址可能不完整。出发前请 **务必先打电话确认**。
- 在线机构在搜索后会被缓存到你的设备上，因此后续常规 **搜索** 也会包含它们。
- 这是应用除地图瓦片下载和（可选的）翻译模型下载之外，唯一的网络请求。

---

## 设置

点击主屏幕右上角的齿轮图标 ⚙。

**搜索半径** —— 1 到 25 英里之间的滑块。应用只会显示位于这个距离内的机构。

**包含信仰背景的服务** —— 默认 **开启**。信仰背景的机构在名称旁边会显示一个小十字 ✝。如果你只想看非信仰背景的机构，可以关闭此项。

**语言** —— 默认是英语。选择器约列出 59 种语言。当你选择非英语语言时：
- 第一次使用时，手机会下载一个该语言的小型翻译模型（10–30 MB）。这一步需要联网。
- 之后翻译完全在 **你的手机本地** 运行，文本不会离开设备。
- 提供机构的名称、地址和描述会被自动翻译。

要切回英语，请打开 设置 → 语言 → English。

---

## 隐私 —— 应用做什么和不做什么

我们非常重视这一点，特别是因为这款应用的目标人群。

**应用 不会 做的事：**
- ❌ 不会把你的位置保存在手机上，哪怕是临时的。
- ❌ 不会把你的位置发到任何服务器（永远不会）。
- ❌ 不会跨会话追踪你。
- ❌ 不会记录你的活动。
- ❌ 没有账号、没有登录。
- ❌ 不使用任何分析或崩溃上报服务。

**应用 通过 互联网发送的，仅有这几件事：**
- 地图瓦片（地图的图像数据）—— 来自 OpenStreetMap。
- 翻译语言模型文件（首次选择非英语语言时）—— 来自 Google ML Kit。
- 你所在区域的边界框 —— **仅在你点击"在线查找更多"时**，发送给 OpenStreetMap 的 Overpass 查询服务。你的精确位置不会被发送，只发送一个粗略的矩形以便它知道在哪儿查找。

**保存在你手机上的内容：**
- 提供机构列表（这样首次搜索之后，应用即便离线也能继续使用）。
- 你的设置（半径、信仰背景开关、语言）。
- 已翻译的字符串缓存，避免相同短语反复翻译。

仅此而已。任何位置数据都不会被写入手机存储。

---

## 常见情况与解决办法

**"无法获取位置"**
- 确认手机的快捷设置里"位置"已打开。
- 确认应用拥有定位权限。打开 **设置 → 应用 → We Rise DFW → 权限 → 位置**，选择 **仅在使用应用时允许**。
- 如果你在室内，可以靠近窗户再试一次。

**地图上出现"超出范围"提示**
- 本应用专为达拉斯 / 沃思堡地区构建。如果你不在 DFW 范围内，将不会有结果，这是设计如此。

**"在线搜索失败"**
- 检查网络连接（Wi-Fi 或移动数据）。
- OpenStreetMap 服务器有时会对频繁访问的用户进行速率限制。等一会儿再试。

**搜索后列表是空的**
- 在设置中尝试扩大半径（最多 25 英里）。
- 试试 **在线查找更多** 来引入更多机构。

**已选语言但仍显示英文**
- 第一次使用某种语言时，下载语言模型可能需要一点时间，请确保第一次下载时联网。
- 如果翻译始终没启动，可能是所选语言不被设备端翻译器支持。换一种语言或保留英语即可。

**营业时间看起来不对**
- 营业时间会变。出发之前请 **务必先电话确认**，特别是接收时段有限的庇护所或食物援助点。

---

## 危机求助热线（DFW）

如果你正面临即时危险或处于危机中，请寻求帮助 —— 以下是覆盖 DFW 地区、24 小时可拨打的电话：

- **988** —— 自杀与危机生命线（电话或短信，覆盖全国）
- **211** —— 德州资源热线（食物、住房、水电费援助）
- **(214) 946-HELP (4357)** —— Genesis Women's Shelter，24/7（家庭暴力）
- **(877) 701-7233** —— SafeHaven of Tarrant County，24/7（家庭暴力）

这些号码同样写入了应用的初始数据里；你可以在相应机构的详情页看到它们。

---

## 来自作者的话

这款应用免费提供，没有注册、没有广告、不期待任何回报。它由一个人独立完成，希望能尽一份微薄之力。如果它能在某个艰难的日子里帮到你，那就是它存在的意义。

如果你是开发者，想要 fork、改进它，或者把它移植到其他城市 —— 欢迎之至。代码采用 MIT 许可证；详情见 [LICENSE](LICENSE)。技术性 README 在 [README.zh.md](README.zh.md)。

照顾好自己。

—— Shane W. Watson
